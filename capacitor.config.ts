import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic Multi Language',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
